pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo build the project'
            }
        }
        stage('Test') {
            steps {
                sh 'echo test the project'
            }
        }
        stage('Deploy') {
            steps {
                sh 'echo deploy the project'
            }
        }
    }
}